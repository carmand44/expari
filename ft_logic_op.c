#include "ft_exp_ari.h"

static ft_check_op(char *str, int size)
{
	int		i;

	i = 0;
	if ()
}

void		ft_logic_op(char *str, int size)
{
	int i;

	i = 0;
	while (i < size)
	{
		if (ft_lower_op(str[i] && (str[i] < '0' || str[i] > '9')
		{

		}

		i++;
	}
}
